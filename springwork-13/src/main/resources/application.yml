spring:
  datasource:
    url: jdbc:h2:mem:h2db
  flyway:
    enable: true
    locations: db/migration
  jpa:
    generated-ddl: false
    hibernate:
      ddl-auto: none

      show-sql: true
  logging:
    root: info
    org.springframework: info
